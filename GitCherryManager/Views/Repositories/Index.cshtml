@model IEnumerable<GitCherryManager.Models.ContextModels.Git.Repository>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Url)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Url)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RepositoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RepositoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RepositoryId })
        </td>
    </tr>
}

</table>
